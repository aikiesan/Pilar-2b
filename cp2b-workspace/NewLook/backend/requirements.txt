# FastAPI and ASGI
# Updated for security patches (Sprint 4 Production Hardening)
fastapi==0.141.1  # Updated from 0.115.7 — pulls starlette>=0.49.1 (fixes CVE-2025-54121, CVE-2025-62727)
uvicorn[standard]==0.52.4  # Updated from 0.24.0 - stability improvements
slowapi==0.1.10

# Database
sqlalchemy==2.0.52
psycopg2-binary==2.9.12
alembic==1.19.1

# Authentication
# python-jose removed: library is unmaintained and has unfixed CVEs (algorithm confusion).
# Local, self-hosted auth (UNICAMP VM): passlib[bcrypt] for password hashing and
# PyJWT for signing/verifying HS256 access tokens with the local SECRET_KEY.
passlib[bcrypt]==1.7.4
# passlib 1.7.4 is incompatible with bcrypt >= 4.1 (removed __about__ +
# changed 72-byte handling break passlib's backend detection) — pin explicitly.
bcrypt==4.0.1
PyJWT==2.13.0  # >=2.13.0 fixes CVE-2026-48525 (DoS via unbounded Base64URL decoding)
python-multipart==0.0.32  # >=0.0.31 fixes CVE-2026-53537/53539/53540 (DoS / request smuggling)

# Environment and Configuration
python-dotenv==1.2.3
pydantic>=2.13.4
# 2.14.2 is the fix for SFTY-20260619-43815 (path traversal via symlink
# handling); the advisory covers >=2.12.0,<2.14.2.
pydantic-settings==2.15.0
email-validator==2.3.0

# Geospatial
geoalchemy2==0.20.0
shapely==2.1.2
geojson==3.3.0
geopandas>=1.1.3  # Updated from 0.14.1 — fixes SQL injection in to_postgis() (GHSA-341)
fiona>=1.9.6      # Updated from 1.9.5 — fixes CVE-2023-45853 and CVE-2020-14152 (bundled zlib)
pyproj==3.7.1
rasterio==1.4.4
pillow>=11.0.0  # Updated from 10.4.0 — fixes out-of-bounds write in PSD image loader

# Data processing
pandas==2.3.3
numpy==1.24.3
scipy==1.15.3
scikit-learn>=1.7.2
openpyxl==3.1.5
xlrd>=2.0.2  # Required to read IBGE .xls workbooks such as POP2025
PyYAML>=6.0.1  # Canonical parameter database (data/canonical_parameters/*.yaml) + generator

# HTTP client
httpx==0.28.1
requests==2.34.2  # Updated from 2.31.0 — fixes CVE-2024-35195, CVE-2024-47081, CVE-2026-25645


# Development and Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-mock==3.15.1
httpx==0.28.1
black>=26.5.1  # Updated from 23.12.1 — fixes arbitrary file write via unsanitised cache file name
isort==8.0.1
