# July 2026 playbook — Consolidation & first real ingest

_Arrive Brazil Jul 9. Prepared-in-advance items are marked ✅ (done remotely)._

## Already done (2026-07-03/04, remote sessions)

- ✅ PR #137 merged (CI fixes: PyJWT CVE, OGC WMS names, compose placeholders)
- ✅ GitGuardian incident 34365115 resolved as false positive
- ✅ Ingestion framework `backend/ingest/` + 48 tests + `INGESTION_GUIDE.md`
- ✅ PR template with data-lineage checklist
- ✅ Migration 021 drafted (`app/migrations/021_national_spine_staging.sql`) —
  apply in August, or earlier if the ANEEL promote wants `validation_plants`
- ✅ PR #139 merged (Brazil expansion foundation: roadmap + ingestion framework)
- ✅ PR #140 merged (Leaflet zoom-smoothness perf pass + month playbooks +
  migration 021 draft)

## Week 1 (Jul 9–12) — land, merge, verify

1. Rebase + merge the open dependabot PRs one at a time on green CI:
   **#115** (actions/checkout v6→v7), **#138** (backend minor-and-patch group —
   supersedes the earlier #119), **#120** (frontend minor-and-patch group).
   For each: re-run backend/frontend suites locally first.
   _(As of 2026-07-04 these three are the only open PRs; #137/#139/#140 are
   already merged.)_
2. Verify production still healthy after merges:
   `curl -s https://cp2b.unicamp.br/pilar2b/... health endpoints`, spot-check
   the live map.
3. Confirm zoom feels smooth on production data after the perf pass deploys.

## Week 2 (Jul 13–19) — resolve the three data discrepancies (papers depend on these)

Each closes with a one-page note in `docs/data/` + a METADATA.json edit:

4. **MapBiomas collection**: decide C10.1 as the single collection.
   - `grep -rn "Collection 8\|collection_8\|col8" backend/app/services/mapbiomas_service.py`
   - Update code constants + `FOSS4G_PAPER_SUPPLEMENT.md` + METADATA.json
     (`collection`, DOI 10.58053/MapBiomas/JNJGVT — confirm for 10.1).
   - Note: the C10/10.1 transition matrices (2008–2024, 15 classes) you
     already have are the October ingest — this step only settles *naming*.
5. **IBGE reference years**: document per-stream — crops = PAM 2023,
   livestock = Censo Agro 2017 (upgrade path: PPM yearly, August). Remove the
   VERIFY flags with the actual retrieval dates.
6. **ANEEL unit audit (19.69 vs 6.39)**: re-derive both numbers from the raw
   SIGA CSV. `MdaPotenciaOutorgadaKw` is kW; sum biomass rows → kW/1e3 = MW,
   /1e6 = GW. Write which published figure was right in
   `docs/data/ANEEL_UNIT_AUDIT.md`. The loader already enforces the kW→MW
   conversion in one place.

## Week 3 (Jul 20–26) — first real ingest: ANEEL SIGA

Follow `docs/data/INGESTION_GUIDE.md` step by step; the source code exists.

7. Download the SIGA "empreendimentos em operação" CSV →
   `backend/data/raw/aneel_siga/2025/siga_operacao.csv`.
8. Fill METADATA.json `aneel_siga.retrieved` (today's date) + reference year.
9. Confirm `COLUMN_MAP` matches the export header
   (`head -1 siga_operacao.csv`); adjust if ANEEL renamed columns.
10. Get ANEEL's published biomass-capacity totals (SIGA dashboard) and wire
    them into `validate()` via `published_totals` — this certifies the unit
    audit from step 6.
11. Run: `python -m ingest.runner run aneel_siga --year 2025`. Iterate until
    all gates pass; commit the report + open the PR (data-lineage checklist).
12. Apply migration 021 to the dev DB, implement `promote()` (staging →
    `validation_plants`, record in `staging.ingest_runs`), add the
    integration test against the CI PostGIS service container.

## Week 4 (Jul 27–31) — close the month

13. Cross-check: `validation_plants` (SIGA) vs the `anp_biometano` CSVs
    already in `analysis/data/` — plant-overlap cross-source gate.
14. Fill the July column of the roadmap indicator table; update
    IMPROVEMENT_BACKLOG.md with anything discovered.
15. Read August's playbook; pre-download IBGE PAM/PPM/SNIS national exports.

## Exit criteria

- [ ] 3 open dependabot PRs (#115, #138, #120) merged, main green
- [ ] 3 discrepancy notes merged; METADATA.json has ≤ 8 VERIFY placeholders
- [ ] ANEEL SIGA promoted through all 8 gates; report committed
- [ ] Migration 021 applied; `SELECT sum(municipality_count) FROM states` = 5570
- [ ] Zoom smoothness confirmed on production
